fedora-ansible-docker
=====================

Docker images of Fedora with ansible preinstalled so that it can be used to
construct the image.

This is similar to the ansible-docker-base repository but with Fedora images as
the starting point


